{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable && ./flutter/bin/flutter build web --release --dart-define=API_KEY=$API_KEY",
  "outputDirectory": "build/web",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
